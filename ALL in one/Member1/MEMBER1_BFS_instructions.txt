Just simply write 16-209 of the main game code.
 

================================================================
AI CHASE GAME — MEMBER 1 INSTRUCTION MANUAL
Algorithm : BFS (Breadth-First Search)
Your Branch: bfs_[your_student_ID]
================================================================

WHAT YOU HAVE TO DO
--------------------
You will be provided with a file called member1_BFS_code.py
This file contains YOUR section of the game code.

Your job is simple:
  1. Set up Git on your computer
  2. Create your branch
  3. Open your code file in VS Code
  4. Rewrite and improve the code (tasks below)
  5. Push your code to GitHub
  6. Open a Pull Request

You only work with the file you were given.


STEP 1 — INSTALL WHAT YOU NEED
--------------------------------
Make sure you have these installed:

  Python  : https://www.python.org/downloads
  Git     : https://git-scm.com/downloads
  VS Code : https://code.visualstudio.com

After installing Python, install pygame:
  pip install pygame


STEP 2 — SET UP GIT
---------------------
Open terminal (or Git Bash on Windows) and run:

  git clone https://github.com/TA-Shaim-12/Multi-Agent-Intelligent-Chase-Game
  cd Multi-Agent-Intelligent-Chase-Game
  git checkout -b bfs_[your_student_ID]
  git push -u origin bfs_[your_student_ID]

Replace [your_student_ID] with your real student ID.


STEP 3 — OPEN YOUR CODE IN VS CODE
-------------------------------------
1. Open VS Code
2. Open the folder: Multi-Agent-Intelligent-Chase-Game
3. Copy the file member1_BFS_code.py into this folder
4. Open it in VS Code
5. Read it fully before making any changes


STEP 4 — YOUR TASKS
---------------------
All changes go into member1_BFS_code.py

TASK 1 — Add comments to bfs_path function
  Find the function called bfs_path
  Add a comment above every line inside it explaining what it does
  A comment starts with the # symbol
  Example:
    # start BFS from the initial position
    # check if we already reached the goal
    if start == goal: return [], 0

TASK 2 — Add two methods to AlgorithmAnalyzer class
  Find the class called AlgorithmAnalyzer
  Add these two methods inside the class after the last method:

    def reset(self):
        self.stats = {a: AlgoStats(a) for a in ALGORITHMS}
        self.session_log.clear()

    def best_algo(self) -> str:
        active = {a: s for a, s in self.stats.items() if s.calls > 0}
        if not active: return "N/A"
        return max(active, key=lambda a: active[a].efficiency)

TASK 3 — Add a new map inside generate_map function
  Find the function called generate_map
  Find the dictionary called cfg inside that function
  Add this new line inside the cfg dictionary:
    "Ruined City": ([WALL, STONE, WALL, MUD], 0.24),

  Then find the list called MAPS near the top of the file
  Add "Ruined City" to that list:
    MAPS = ["Forest Chase","Rocky Desert","Muddy Swamp",
            "Urban Grid","Mountain Pass","Ruined City","Custom"]

TASK 4 — Write your analysis
  Create a new file in the same folder called README_member1.md
  Write the comparison between BFS vs Dijkstra algorithm
  Topics to cover:
    - Which algorithm is faster
    - Which explores more nodes
    - Which finds a better path on terrain maps like mud or stone
    - Does BFS care about terrain cost or not


STEP 5 — PUSH YOUR CODE TO GITHUB
------------------------------------
After each task, save your file and run these commands:

  git add member1_BFS_code.py
  git commit -m "bfs: add comments to bfs_path"
  git push

Repeat after every task. You need at least 5 commits total.

Commit message examples:
  git commit -m "bfs: add comments to bfs_path"
  git commit -m "bfs: add reset and best_algo methods"
  git commit -m "bfs: add Ruined City map"
  git commit -m "bfs: write analysis in README_member1.md"
  git commit -m "bfs: final review and cleanup"


STEP 6 — OPEN A PULL REQUEST
------------------------------
When all tasks are done:
  1. Go to: https://github.com/TA-Shaim-12/Multi-Agent-Intelligent-Chase-Game
  2. Click Pull requests tab
  3. Click New pull request
  4. Base: main   Compare: bfs_[your_student_ID]
  5. Click Create pull request
  6. Title: [Member 1] BFS done
  7. Click Create pull request

STOP. Do NOT click Merge.


DO NOT DO THESE THINGS
------------------------
- Do not edit the main branch directly
- Do not click Merge on your Pull Request
- Do not delete your branch after submitting
- Do not share your code file with other members


IF YOU GET STUCK
-----------------
Git not recognized:
  Restart your terminal after installing Git

Permission denied when pushing:
  Accept the GitHub invitation the leader sent to your email

Game crashes when you run it:
  Read the red error message, it tells you which line is wrong
  Fix that line and run again

Made a mistake in a commit:
  Just fix the file and make a new commit with a new message

================================================================
================================================================
